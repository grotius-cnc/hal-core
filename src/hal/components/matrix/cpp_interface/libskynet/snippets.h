#ifndef SNIPPETS_H
#define SNIPPETS_H

// Just a connect example:
// QObject::connect(ui->checkBox_rt_motion, SIGNAL(pressed()),SLOT(rt_motion()));
// QObject::connect(ui->spinBox_max_vel_cart, SIGNAL(valueChanged(int)),SLOT(set_maxvel_cart(int)));

#endif // SNIPPETS_H
